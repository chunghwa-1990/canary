<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.canary.sys.mapper.RolePermissionMapper">


    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO sys_role_permission (id, role_id, permission_id)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (REPLACE(UUID(), '-', ''), #{item.roleId}, #{item.permissionId})
        </foreach>

    </insert>
    <select id="selectPermissionsByRoleIds" resultType="com.example.canary.sys.entity.PermissionPO">
        SELECT DISTINCT
            p.*
        FROM
            sys_permission p
                INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
                AND rp.is_deleted = '0'
        WHERE
            p.is_deleted = '0'
          AND p.is_disabled = 0
          AND rp.role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")" >
            #roleId
        </foreach>
    </select>

    <select id="selectPermissionsByUserId" resultType="com.example.canary.sys.entity.PermissionDTO">
        SELECT DISTINCT
            p.id,
            p.name,
            p.icon,
            p.route,
            p.is_disabled as disabled,
            mp.menu_id
        FROM
            sys_permission p
                INNER JOIN sys_menu_permission mp ON p.id = mp.permission_id
                AND mp.is_deleted = '0'
                INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
                AND rp.is_deleted = '0'
                INNER JOIN sys_role r ON rp.role_id = r.id
                AND r.is_deleted = '0'
                AND r.is_disabled = 0
                INNER JOIN sys_user_role ur ON r.id = ur.role_id
                AND ur.is_deleted = '0'
        WHERE
            p.is_deleted = '0'
          AND p.is_disabled = 0
          AND ur.user_id = #{userId}
    </select>


</mapper>

